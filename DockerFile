FROM python:3.10-slim
WORKDIR /app
# Копіюємо файл залежностей (переконайся, що назва правильна!)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
# Копіюємо всі інші файли
COPY . .
# ВИПРАВЛЕНО: GhosstyCode.py (дві 't')
CMD ["python", "GhosstyCode.py"]
