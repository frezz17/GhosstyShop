version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: ghosty-shop-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - TZ=Europe/Kiev
    volumes:
      - ./data:/app/data
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
